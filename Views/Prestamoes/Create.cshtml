@using Biblioteca.Models.ViewModels
@model PrestamoViewModel

@{
    ViewData["Title"] = "Generar Prestamo";
}

@section Styles {
    <link rel="stylesheet" href="~/css/administrar-elementos.css" />
}

<h1>Generar Nuevo Prestamo</h1>

<h4>Prestamo</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="PrestamoCreate.FinPrestamo" class="control-label"></label>
                <input asp-for="PrestamoCreate.FinPrestamo" class="form-control" />
                <span asp-validation-for="PrestamoCreate.FinPrestamo" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="PrestamoCreate.UsuarioId" class="control-label"></label>
                <select asp-for="PrestamoCreate.UsuarioId" class="form-control" asp-items="Model.SelectListsUsuarios"></select>
            </div>
            <div class="form-group">
                <label asp-for="PrestamoCreate.LibroId" class="control-label"></label>
                <select asp-for="PrestamoCreate.LibroId" class="form-control" asp-items="Model.SelectListsLibros"></select>
            </div>
            <br />
            <div class="form-group">
                <input type="submit" value="Agregar" class="btn btn-dark" />
            </div>
        </form>
    </div>
</div>
<br />
<div>
    <a asp-action="Index" class="btn btn-dark">Volver</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
